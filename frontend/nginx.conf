server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Angular SPA - minden route az index.html-re mutat
    location / {
        try_files $uri $uri/ /index.html;
    }
}
